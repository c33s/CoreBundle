knp_gaufrette:
    adapters:
        local_base:
            local:
                directory:  '%kernel.root_dir%/../web/media/storage/'

    filesystems:
        web_default_fs:
            adapter:    local_base
