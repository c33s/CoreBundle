{{ name|lower }}_{{ bundlename | underscore }}_redirect_example:
    path:     /example/
    defaults:
        _controller: FrameworkBundle:Redirect:urlRedirect
        path: /example-new/
        permanent: false


################################################################################
# Legacy Hompage Redirects
################################################################################

{{ name|lower }}_{{ bundlename | underscore }}_redirect_legacy_1:
    path:     /legacy.html
    defaults:
        _controller: FrameworkBundle:Redirect:urlRedirect
        path: /
        permanent: true
