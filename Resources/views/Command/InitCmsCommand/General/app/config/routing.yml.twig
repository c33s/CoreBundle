_imagine:
    resource: .
    type:     imagine

{{ name.underscored }}_app:
    resource: routing_app.yml
#    host:     "{hostname}"
#    defaults: { hostname: %master_domain% }
#    requirements:
#        hostname:     example.com|example.com.dev|localhost|staging.example.com